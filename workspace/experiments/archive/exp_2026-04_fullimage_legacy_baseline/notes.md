# Notes

## 当前判断

- 历史结果是项目后续 ROI-only 实验的直接基线
- 历史结果已经物理迁入本实验目录，后续不再继续使用旧 `runs/` 结构
- ROI-only 分类实验将直接以本归档实验为对照，不再额外建立一套旧路径兼容层

## 风险

- 根目录 `data/` 仍残留一个被系统占用的空目录壳，后续可再清理
- 历史脚本说明文本中仍可能出现 `scripts/`、`runs/` 的旧表述，需要逐步替换
