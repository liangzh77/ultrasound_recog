# S1a临床确认机器门结果

## 结论

临床确认机器门已实现并通过空白模板正式审计。当前模板完整覆盖八项医学决定、八项复核参数、双角色签署、源/完成工作簿双哈希、数据和标注版本；隐私合同通过。

审计结果按设计为`ready_for_preregistration=false`，因为医生尚未填写回传表。这是预期的安全阴性结果，不是失败：它证明空白表无法被误认为正式临床确认，也不会自动修改复核配置。

## 研究问题与预设门槛

研究问题：能否在临床答案未知时预先建立一个机器可验证、不可绕过且不泄露身份信息的S1a冻结门？

预设门槛：

- Q1～Q8和P1～P8必须准确且唯一；
- 数据指纹、标注版本、空白源表和医生回传表哈希必须匹配；
- 完成工作簿必须是合法XLSX并含五个规定工作表，且哈希不能等于空白表；
- 完成工作簿中的Q1～Q8选择、操作性定义、理由、逐行角色/日期以及P1～P8最终值/理由必须与确认YAML逐项一致；
- 医学定义、理由、参数值、临床角色和研究角色签署必须完整；
- 姓名字段、患者/图像键、私有/原始路径和绝对路径必须被拒绝；
- 偏离建议的方法选择必须引用真实存在的ADR；
- 当前代码不能表达的复核方案只能报告“不兼容”，不得生成冻结补丁；
- 验证器只能生成建议补丁，不得改写现有YAML；
- 空白模板必须验证通过但保持不可冻结。

## 输入与版本

| 项目 | 值 |
|---|---|
| 数据指纹 | `62ecb01c4d77ec0012704611ecc8d18ef51ebb4e0ea744fb3896948829f0b675` |
| 标注版本 | `annotations_62ecb01c4d77` |
| 复核配置SHA-256 | `ffe31cdcb59a3b7fe55f30cdcf99d49def93ae754b490bdd629344107478a17c` |
| 空白工作簿SHA-256 | `b0247dc30cbd75a3403baeff9e8475ab1a5063fd4826801cce0afb43f3b6aab2` |
| 确认YAML | `configs/research/annotation_clinical_confirmation_v0.yaml` |
| 确认YAML SHA-256 | `9abab1f7e2f783f3da451a000c1e5dce959976ab64a2e76073bfeacb86b9d0e7` |
| 合同实现Git | `4e90b11558793f322c2159603d3e0955278e2bc9` |
| CLI实现Git | `af257e3488208dba9c09e1da525cba0da2455a86` |
| 安全加固Git | `b714d04bf63c5ba520f1d2030c5e85062f74742b` |
| 审计持久化Git | `6e5f0248b0d0bede39f62841aa9c7378d6499938` |
| 只读工作簿提取Git | `e018097b1cb39a016d74787bc4e6476487c4a91c` |
| 工作簿/YAML对账Git | `683a005dc4950eca9d45d236d7f7799ec1886f26` |
| 正式复核录入工程Git | `f5abe665bb2cb3caf0c5d1ab3e6d4c496ca2602f` |
| 最新正式审计Git | `f5abe665bb2cb3caf0c5d1ab3e6d4c496ca2602f` |

## 正式空白模板审计

命令：

```text
python tools/38_validate_annotation_clinical_confirmation.py --template-check
```

从干净Git `f5abe665bb2cb3caf0c5d1ab3e6d4c496ca2602f`运行，结果：

| 项目 | 结果 |
|---|---:|
| 医学问题 | 8/8 |
| 复核参数 | 8/8 |
| 隐私合同 | 通过 |
| XLSX容器 | 合法 |
| 规定工作表 | 5/5 |
| 可冻结 | 否（预期） |
| 运行时间 | 0.1567秒 |

报告为`reports/annotation_review/annotation_clinical_confirmation_validation_9abab1f7e2f7.json`，SHA-256 `0e1f8645a9278bf09febd5c9c650a23a89128cc6820de373b8df52ede2c4d9cf`。报告反向索引确认YAML、复核配置、工作簿、数据、标注和Git；不含姓名或原始路径。

严格模式现在只读解析固定工作表和单元格，兼容共享字符串、内联字符串、数值/布尔值及Excel日期序号；人名在读取边界立即降为“是否存在”，不进入返回值或错误信息。Q1～Q8的人类选项字母映射为机器代码后，与YAML中的选项、定义和理由逐项对账；P1～P8按类型对账；逐行角色/日期、双签署存在性和最终日期也必须通过。任一差异只报告安全的题号/字段名并失败关闭。真实回传表尚未收到，因此这次没有伪造严格模式通过结果；实现及阴性路径见[独立对账检查点](S1a临床确认工作簿对账检查点_2026-08-03.md)。

## 成功与停止逻辑

完成确认时，CLI还要求`--completed-workbook`。全部字段通过后才可能生成只读配置建议，包括：

- `status: frozen_preregistered`；
- 复核人数、bootstrap次数和三项一致性阈值；
- 每目标旧阳性/旧未标候选数量及每折配额；
- 任一核心分歧强制裁决。

以下情况不会生成补丁：回传表等于空白表、不是有效XLSX、少工作表、双标比例低于100%、复核者不是2人、总量不能按当前8目标/2桶/5折合同平衡、取消强制裁决、改变当前CI门槛实现，或方法偏离没有真实ADR。合法但不可兼容的确认会写版本化阴性报告并返回非零退出码。

## 代码审查与验证

按正确性、可读性、架构、安全和性能五轴审查：

- 正确性：推荐路径、缺失定义、非法选项/范围、版本不符、空白表冒充回传表、Excel/YAML选项及参数不一致、逐行角色缺失、偏离建议、合同不兼容均有行为测试；
- 可读性：医学选项、流程参数和允许角色由集中常量定义，CLI只负责文件边界和报告；
- 架构：复用既有`sha256_file`、研究路径和低优先级运行合同；不引入依赖、不改训练代码；
- 安全：YAML/XLSX/XML均有限制大小；XLSX只读解析固定页/格；输出限制在研究报告目录；姓名只转为存在性布尔值，错误仅列安全字段编号；
- 性能：只解析小型YAML、共享字符串和三个固定工作表，CPU低优先级，不使用GPU。

临床门与工作簿对账专项24项通过；含正式录入工程的完整测试177项通过，Python编译检查通过。审查中发现并修复：源/完成表哈希混用、伪ADR路径可提前判定ready、未限制外部文件大小、输出可指向项目任意位置、不同确认版本可能互相覆盖、Excel/YAML此前只人工对账，以及工作流Git若自引用将永远不可满足等问题。无未解决Critical或Important项。

## 局限性

当前CLI能够自动比较固定填写单元格与YAML，但不会把Excel自动写成YAML，也不会自行解释临床自由文本。研究负责人仍需建立新的完成版YAML并完成第二角色签署；机器门只验证两份受控记录一致，不替代临床判断。真实完成工作簿尚未收到，所以严格门的真实数据运行仍待完成。

## 阶段完成审计

- [x] 研究问题、预设门槛和停止条件已在结果未知时固定；
- [x] 数据、标注、Git、配置、工作簿、确认YAML和审计报告哈希可复核；
- [x] 运行时间、低优先级CPU和无GPU已记录；
- [x] 正向、阴性、非法和合同不兼容路线均有测试；
- [x] 无姓名、患者标识、图像键或原始路径进入报告；
- [x] Excel/YAML逐项对账已实现，差异只输出安全字段编号；
- [x] 重要冻结决定写入[ADR-011](../decisions/ADR-011-临床确认通过机器门后才允许冻结S1a.md)；
- [x] 本阶段无fold/seed、权重、OOF和MLflow的原因明确；
- [ ] 临床答案和完成工作簿尚未收到，S1a仍不可冻结。

## 下一步

收到医生填写表后保留原件并计算完成工作簿SHA-256，把Q1～Q8、P1～P8转录到新的确认YAML版本，填写角色代码和日期，然后执行严格模式。机器门将自动逐项对账Excel和YAML；只有输出`ready_for_preregistration=true`后，才允许依据建议补丁人工生成新的复核配置、重新生成队列并计算新哈希。
