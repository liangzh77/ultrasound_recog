# 区域/病变标注临床本体与补充标注协议草案

## 状态与目的

状态：`Draft / clinical review required`。本文落实ADR-010的S1a，不是最终训练预登记。它把现有28类转换成可由超声医生逐项确认的本体和标注规则；所有含义未确认项保持待定，不能先训练后解释。

机器可读草案为 `configs/research/annotation_ontology_review_v0.yaml`，SHA-256为`41208a3d47c38c446e6ccdea9ebbe69d9c48e417cb358e88a866e3892646fdc8`。它与数据指纹`62ecb01c4d77ec0012704611ecc8d18ef51ebb4e0ea744fb3896948829f0b675`、标注版本`annotations_62ecb01c4d77`绑定。

## 为什么不能只按现有类名训练

S1显示4,346对跨类多边形重叠，9类多折支持不足；人工标注存在性本身又能以macro-F1 0.8240区分主要诊断。这意味着类名同时混合了解剖结构、病变、切面选择和标注者已知诊断后的选择。

现有名称也与标准病变不完全一一对应。EULAR-OMERACT把RA超声滑膜炎拆为灰阶滑膜增厚和滑膜内Doppler活动，并形成组合评分；“滑膜”不能在未确认时自动解释成“滑膜增生”。痛风OMERACT基本病变包括痛风石、聚集体、双轨征和骨侵蚀；泛称“结晶”不能确定是哪一种。膝OA超声可靠性研究则分别评价滑膜、骨赘、软骨和半月板异常，正常“软骨”轮廓不能自动等同于软骨损伤。[RA滑膜炎定义研究](https://pubmed.ncbi.nlm.nih.gov/28948983/)、[痛风基本病变验证](https://pubmed.ncbi.nlm.nih.gov/26329333/)、[痛风病变一致性研究](https://pubmed.ncbi.nlm.nih.gov/26472419/)、[膝OA超声可靠性研究](https://doi.org/10.1136/annrheumdis-2014-206774)。

## 建议本体

### A. 解剖定位层

候选包括骨皮质、软骨、髌韧带、股四头肌腱、内侧/外侧副韧带、内侧半月板、髂胫束、股二头肌腱、鹅足腱、半膜肌/腱和髌前浅筋膜。

用途仅为ROI内定位、切面辅助或病变相对位置，不把“是否画了某结构”直接作为诊断特征。这里尤其要确认：

- `软骨`标的是所有可见正常软骨，还是只标异常软骨；
- `骨皮质`标的是可见骨面，不能混入骨侵蚀或骨赘；
- 结构只在特定切面出现时，未出现应记`not_in_view`而不是病变阴性。

### B. 炎症活动层

优先复核滑膜血流、积液；`滑膜`必须先确认是解剖滑膜还是滑膜增厚/增生。建议目标状态分别记录，不把积液并入滑膜增厚。Doppler目标还必须记录图像模式及“信号位于滑膜内”的判定。

### C. 结构损伤层

骨侵蚀、骨赘为支持充分的候选；髌前血肿支持不足，暂缓。`软骨`只有在医生确认其表示损伤而非正常结构后，才能转入此层。

### D. 晶体沉积层

痛风石与`结晶`不能只按当前名字直接训练。医生需要把每个`结晶`实例映射为双轨征、聚集体、其他钙化或不确定；痛风石应按一致的形态定义复核。由于这两类当前只见于痛风主要诊断患者，复核尽量对诊断盲法，并加入非痛风可见负例。

### E. 囊肿/滑囊层

腘窝囊肿支持充分，可作为独立候选。囊肿、滑膜囊肿、半月板囊肿、髌下深囊炎、鹅足腱滑囊/炎支持不足或含义冲突；先确定上位类“囊性/滑囊性病变”是否临床有用，再决定保留子类还是合并。客户要求排除的是“滑膜囊肿”主要诊断患者，不自动等于必须删除所有同名局部病变对象，但当前纳入队列仅1名患者，不能独立建模。

## 每张图必须新增的状态

对进入新训练合同的每个目标，不能只有“有多边形/无多边形”。至少需要：

| 字段 | 允许值 | 含义 |
|---|---|---|
| `presence_state` | `present` | 目标明确存在；有多边形或明确图级阳性 |
|  | `absent_visible` | 对应解剖区域可见、图像质量足够、医生确认没有目标；唯一可作负例的状态 |
|  | `not_in_view` | 切面没有覆盖目标位置，不是负例 |
|  | `uncertain` | 医生无法可靠判断；训练损失mask掉 |
|  | `legacy_unknown` | 旧数据未说明是否穷尽；训练损失mask掉 |
| `annotation_scope` | `exhaustive_for_declared_targets` | 对声明目标完成逐图穷尽审阅 |
|  | `positive_only` | 只画了看到的阳性，不允许把缺失类当阴性 |
|  | `unknown` | 旧标注默认值 |
| `review_status` | `single` / `double` / `adjudicated` | 标注一致性与裁决状态 |
| `image_mode` | `B` / `PD` / `CD` / `unknown` | 灰阶、能量或彩色Doppler |
| `truncated_by_roi` | true / false | 多边形是否碰到或越过ROI边界 |

## 重叠、ROI和负例规则

- 不同类别允许像素重叠；原始实例多边形分别保存，不以绘制顺序覆盖；
- 需要语义mask时，每个目标单独一个二值通道，或按临床本体拆成不同任务头；
- 只读取人工确认ROI内像素。越界多边形与ROI求交，保留`truncated_by_roi=true`并进入复核队列；交集为空时不静默变成阴性；
- 修复无效多边形后重算bbox和面积，旧`area`字段不作为训练真值；
- 只有`absent_visible`参与负样本损失；`not_in_view`、`uncertain`和`legacy_unknown`全部mask掉；
- 用于诊断的任何病变预测必须由固定患者外层fold内训练产生OOF，禁止使用人工存在性或全数据模型预测。

## 建议的最小临床复核批次

在不修改原标注的前提下，先建立一个300～500张的伪匿名复核集，覆盖五折、六类主要诊断、B/PD模式和阳性/候选负例。建议优先问题：

1. `滑膜`到底是正常结构还是滑膜增厚；
2. `结晶`是否可拆为双轨征/聚集体/其他；
3. 积液、滑膜血流、骨侵蚀、骨赘和腘窝囊肿能否做到逐图`absent_visible`；
4. 囊肿/滑囊相关稀有类是否合并；
5. ROI边界截断实例是否仍可可靠评价。

其中至少一部分由两名医生独立复核并裁决。S1a图级语义复核报告存在性一致率、阳性一致率和Cohen's kappa；S1a通过后，S1b再对独立新绘几何报告Dice/IoU、可比较阳性对和边界失败原因。实际张数、双标比例和一致性门槛需由临床资源确认后写入各自正式预登记，不能在结果出现后补设。分阶段理由见[ADR-012](../decisions/ADR-012-拆分S1a语义复核与S1b几何可靠性复核.md)。

## 进入训练前必须回答的八个问题

1. `滑膜`的操作性定义是什么？
2. `滑膜血流`使用PD还是CD，是否要求信号位于已确认滑膜内？
3. `结晶`如何映射到双轨征、聚集体或其他钙化？
4. `软骨`是正常结构还是损伤？
5. `骨皮质`是否只表示定位结构？
6. 五个囊肿/滑囊相关名称如何建立上位和子类关系？
7. 旧数据哪些目标是逐图穷尽标注，哪些只是阳性记录？
8. 医生能否在不看主要诊断的条件下完成复核集？

八项没有形成书面答案前，本草案保持Draft；不得把它当成正式训练标签合同。

## 伪匿名复核队列工程草案

不依赖上述医学答案的抽样和隐私工程已完成，但样本量尚未正式预登记：

- 实现Git：`befc44e18b59ff6e20f585cf0d03624bed843001`；
- 配置：`configs/research/annotation_review_queue_v0.yaml`，SHA-256 `f1985c4ad8786afeee93056cfd401d9b05377166883d0d6851f8a0326d379b51`；
- 建议目标：滑膜、滑膜血流、积液、骨侵蚀、骨赘、痛风石、结晶、腘窝囊肿；
- 每目标50图：每个固定fold抽5个旧阳性和5个“旧标注未出现、等待医生判断”的候选；后者明确不是负例；
- 生成400张唯一图，内部覆盖323名患者；每项计划两名医生独立复核后裁决；
- 面向复核者的CSV不含诊断、患者键、fold、旧标注答案、文件名或路径，只含伪匿名图像键、目标和空白复核字段；
- 草案队列：`reports/annotation_review/annotation_review_queue_draft.csv`，SHA-256 `aadc0b412bc958c95b87c89972856d6ef84026375e69bc50e61601895ce022a4`；
- 聚合审计：`reports/annotation_review/annotation_review_queue_draft_audit.json`，SHA-256 `902a9376e2e3f8600dbf6e4bb418b670e61c752f8043c2629eeba8cd6d8e740a`。

上述`reports/...`均位于当前患者级研究实验目录。队列状态为`draft_not_preregistered`：临床方确认目标、实际工作量、双标比例和一致性门槛后必须生成新的冻结版本及哈希；不得直接在当前草案上填写后又把它追认为预登记样本。

### 复核结果验收器

Git `bc41719f1c9c9964e95dc2330e8bef9d3c2daf7c`已实现`tools/36_validate_annotation_review_results.py`：

- 当前草案只能运行`--template-check`；配置未改为`frozen_preregistered`时拒绝正式统计；
- 两名复核者的presence、模式、穷尽范围和子型都必须合法完整；S1a的多边形动作固定为`not_applicable`，不得据此计算几何一致性；
- 任一核心字段不一致都必须有相应裁决和裁决说明；
- 逐目标报告四态原始一致率、Cohen's kappa、仅`present/absent_visible`可比子集的阳性/阴性一致率，以及2,000次行级bootstrap 95% CI；
- 正式配置的一致性门槛必须是预先冻结的数值，当前三个门槛仍为`pending_clinical_confirmation`，因此不能事后追设；
- 输出仅为聚合统计，不复制病例键、医生自由文本或裁决明细；自由文本若含原始路径/身份字段会被拒绝。

当前空白模板检查覆盖400行/400图/8目标并通过；它只证明结构和隐私合同，不产生一致性结果。

### ROI只读预览窗口

Git `812067dbf2d67f31af3e22aded4ce3dce68cb01d`新增独立预览器：

```text
python tools/37_preview_annotation_review_queue.py
```

- 通过私有关联表在程序内部解析`image_key`，界面不显示诊断、患者键、fold、旧标注、原文件名或路径；
- 只解码并显示人工确认ROI内像素，不把ROI外信息送入显示部件；
- 当前只能左右键或按钮浏览，标题和黄色文字均明确标识“草案只读预览、不记录结果、不用于训练”；
- 配置驱动的正式录入尚未开放，避免在医学定义和一致性门槛未冻结时形成伪正式结果；
- 显式加载中文字体，按钮有可见标签和键盘等价操作，读取失败时显示文本错误而不是空白；
- 133项完整测试和真实队列首图offscreen smoke test通过；视觉截图确认中文、ROI比例、状态提示和导航正常；
- 视觉验证截图：`reports/annotation_review/annotation_review_queue_preview.png`，SHA-256 `bd8a34c3d4f87b8a2ef9afa63634e2b3f5db714bad3d86748fd6037715f6f836`。

预览窗口不是正式标注程序，不写CSV或JSON。正式语义录入程序的工程实现已完成，但由`frozen_preregistered`、完成确认报告、队列哈希/行数、干净Git及工作流代码基线共同锁定；当前Draft仍在创建响应前拒绝。工程结果见[S1a正式语义复核录入工程准备结果](S1a正式语义复核录入工程准备结果_2026-08-03.md)。

### 400图ROI全量加载审计

Git `3130d6df17c9110bb414f0cc361223f32fe3e4da`新增`--audit-all`只读检查，并从该干净提交重新生成正式聚合证据：

- 队列400行全部成功加载人工确认ROI，`loaded_rois=400`、`failed_rois=0`；
- ROI宽度范围458～1244像素，高度范围233～711像素；
- 总运行时间4.6109秒，进程使用低优先级，不占用GPU；
- 审计明确记录：显示过程未加载诊断/旧标注，未加载人工确认ROI之外像素；
- 聚合报告：`reports/annotation_review/annotation_review_roi_load_audit.json`，SHA-256 `c329f17eb58160485d2c8e45c7aaf3cd645f4e5867b1e395de46c9e264ba00de`；
- 报告绑定队列SHA-256 `aadc0b412bc958c95b87c89972856d6ef84026375e69bc50e61601895ce022a4`、配置SHA-256 `f1985c4ad8786afeee93056cfd401d9b05377166883d0d6851f8a0326d379b51`、数据指纹和标注版本；
- 完整测试134项通过，`git diff --check`通过。

这只证明复核输入、ROI解码和隐私显示边界在当前400图草案队列上可执行，不证明医学定义、一致性门槛或标注质量已经通过。独立检查点结果见[S1a盲法复核工程准备结果](S1a盲法复核工程准备结果_2026-08-02.md)。

### 临床书面确认工作簿

2026-08-03已把本协议的八项医学问题和八项研究流程门槛转换为可直接回传医生填写的五页工作簿，准备结果见[S1a临床确认包准备结果](S1a临床确认包准备结果_2026-08-03.md)。工作簿仅含研究元数据、推荐选项、字段代码本和公开文献链接，不含任何患者级数据。

当前建议的400图、全部双标、原始presence一致率0.80、二元一致率0.85和Cohen's kappa 0.60均明确标为“建议值”，不是已冻结门槛。只有八项医学定义和八项复核参数全部由临床/研究角色书面确认后，才允许生成新的`frozen_preregistered`配置与队列哈希；不得直接修改本草案状态或沿用当前哈希。

临床确认的机器合同为`configs/research/annotation_clinical_confirmation_v0.yaml`。空白合同SHA-256为`9abab1f7e2f783f3da451a000c1e5dce959976ab64a2e76073bfeacb86b9d0e7`，运行`python tools/38_validate_annotation_clinical_confirmation.py --template-check`可复核。当前正式空白审计通过8项医学问题、8项复核参数、隐私和五页XLSX合同，但按设计保持不可冻结。严格模式、停止条件和只读补丁规则见[S1a临床确认机器门结果](S1a临床确认机器门结果_2026-08-03.md)。

2026-08-03严格门进一步补齐完成工作簿与确认YAML的只读逐项对账：医学选项、操作性定义、理由、逐行角色/日期、复核参数和双签署任一不一致都会失败关闭；姓名不进入机器结果，验证器仍不自动写配置。实现与验证见[S1a临床确认工作簿对账检查点](S1a临床确认工作簿对账检查点_2026-08-03.md)。这项工程完成不改变本文`Draft / clinical review required`状态。

同日完成的合同一致性审计确认：旧多边形完全盲法时无法选择“保留/调整”，现有复核CSV也没有独立几何坐标。因此[ADR-012](../decisions/ADR-012-拆分S1a语义复核与S1b几何可靠性复核.md)把S1a限定为语义/存在性复核；两名复核者使用独立响应文件，合并前互不可见。S1b将在S1a通过后另行预登记独立新绘多边形、Dice/IoU、ROI截断和边界失败。本草案不得再把S1a动作字段解释为几何可靠性证据。
