# H0：G0折间异质性审计工程检查点

## 结论

H0已完成机器可读合同、只读输入连接、全部预登记统计和正式CLI的工程闭环；专项9项与完整230项测试通过。正式统计尚未运行，所有H0结果仍未知。本检查点只授权从干净Git基线运行一次冻结H0，不授权训练新诊断模型、重做G0、融合或查看2026标签。

## 冻结标识

| 项目 | 值 |
|---|---|
| 数据指纹 | `62ecb01c4d77ec0012704611ecc8d18ef51ebb4e0ea744fb3896948829f0b675` |
| 配置 | `configs/research/h0_g0_heterogeneity_audit.yaml` |
| 配置SHA-256 | `bdbf2c4e558faf2755ddf1eca33598a889f2d115d540eb4c8c9ede8aeed48811` |
| 输入冻结与连接提交 | `3c8e2ad` |
| 统计实现提交 | `4bf3aeb` |
| 正式CLI提交 | `0c244ed` |
| seed | `20260724` |
| fold | `0,1,2,3,4` |
| 资源 | CPU-only、数值线程2、目标30分钟、硬限120分钟 |

正式运行的完整Git提交由CLI在结果中自动记录；正式运行只允许从文档提交后的干净工作树启动。

## 已完成合同

- 967名患者、4,543张图、200正常/767异常、五折和数据指纹为硬门；
- G0 OOF、G0评价、代理缓存、既有模型可见性审计和六个代理OOF逐文件核对SHA；
- 六组代理与G0按伪匿名患者键一一连接，fold与正常/异常参考逐行一致；
- H0-A实现全局BH-FDR、SMD、KS、组级广泛偏移和诊断内置换的fold识别；
- H0-B实现总体错误、正常误报、异常漏报的固定fold OOF、200次折内/参考内置换和2,000次分层bootstrap；
- H0-C实现六个既有代理OOF折叠、逐折/pooled Spearman、2,000次置换与bootstrap；
- fold 3单类错误目标保留并记不可估计，稳定性自动失败；
- 输出只含安全聚合或伪匿名键，不读取或输出姓名、源文件名、原始路径、临床表格、身份连接或2026标签。

## 验证证据

计划命令均已实现：

```text
python tools/44_audit_g0_heterogeneity.py --config configs/research/h0_g0_heterogeneity_audit.yaml --dry-run
python -m pytest -q tests/test_research_g0_heterogeneity.py tests/test_tool_44_g0_heterogeneity.py
python -m pytest -q
```

结果：

- dry-run：`READY`；967人、4,543图、五折、六组代理和全部冻结SHA通过；不生成正式结果；
- H0专项：9 passed；
- 完整回归：230 passed，89.72秒；
- Python编译和`git diff --check`通过；本环境未安装ruff，因此没有伪称执行ruff；
- 初次完整回归真实失败1项：实验ledger已有25项/12个正式模型，测试仍写死23/11；修正陈旧断言后专项16项和完整230项通过。该失败未删除或隐瞒。
- 文档—产物审计覆盖56个Markdown、160个相对链接和25项实验登记并通过；审计JSON SHA-256为`9d0ca907083b4e391b894d4f0693a94c0d509467b7053dcbb25aaea7d4791fc5`。

## 正式运行前门

- [x] 项目负责人在结果未知时确认范围与门槛；
- [x] YAML、输入SHA、人数、fold和隐私合同通过；
- [x] 三类分析与单类fold失败关闭测试通过；
- [x] dry-run不生成正式产物；
- [x] 完整测试通过；
- [ ] 本检查点文档提交后工作树干净；
- [ ] 从该干净提交运行一次正式H0；
- [ ] 结果、MLflow、聚合CSV、ledger、独立报告、必要ADR和文档审计相互索引。

## 下一步

提交本检查点后执行一次冻结正式命令。无论结果阳性、阴性、不可估计或运行失败，均立即登记；不因结果修改代理组、门槛、fold或置换层级。
