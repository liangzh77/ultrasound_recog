# G0患者级正常/异常门控工程闭环

## 结论

G0从冻结规格推进到可执行但尚未训练的工程状态。Git基线`59b67d0bf6757be541c57f917cd77f1f0b6586a9`已经闭合患者级二分类标签、训练、inner validation温度/阈值、逐折安全OOF、注意力审计、五折合并、统计门槛、资源门槛、产物哈希和MLflow反查。正式入口不再被实现锁阻塞，但仍要求干净Git、CUDA及资源启动门全部通过。

本检查点没有创建G0 MLflow run、权重或OOF，也没有查看2026标签。下一步只能先运行fold 0五epoch资源pilot；pilot不迭代外层测试，不计算正式结果。

## 冻结输入

- 研究问题：只用一名患者的全部人工确认ROI图像，区分正常与异常；
- 配置：`configs/research/g0_roi_normal_abnormal_gate_b2.yaml`；
- 配置SHA-256：`4a06e647e6b1ba5e4223a4ec752110bf71b9bfc257e8b134271c508c9c53ed72`；
- 数据指纹：`62ecb01c4d77ec0012704611ecc8d18ef51ebb4e0ea744fb3896948829f0b675`；
- 队列合同：967名患者、4,543张图像、200正常、767异常、固定五折；
- 唯一候选：ROI letterbox 384、EfficientNet-B2、gated-attention MIL、KL权重0.05；
- 资源合同：4个CPU线程、2个DataLoader worker、9GB显存软预算、11.5小时软限和23.5小时硬限；
- 隐私合同：不读取Excel、Excel存在性、2026标签、姓名、源文件名或原始路径。

## 已完成的工程增量

| Git | 内容 | 主要失败关闭行为 |
|---|---|---|
| `1a3ab6e` | 定义G0后处理公共接口 | 只允许inner validation拟合温度和阈值 |
| `3dd97a6` | 增加训练入口与真实registry dry-run | 数据人数、图像数和正常/异常数漂移即拒绝 |
| `a339275` | 强制冻结配置哈希 | 配置字节变化即拒绝 |
| `433f1b9` | 接入单折训练、校准OOF和产物记录 | dirty Git、无CUDA或资源启动门失败即拒绝正式运行 |
| `9acf5ff` | 提取可复用注意力OOF审计 | 图像重复、权重和不为1、人数/折不一致即拒绝 |
| `59b67d0` | 完成五折正式评估器并解除实现锁 | 五折/seed/Git/MLflow/哈希/外测隔离/资源任一不符即拒绝 |

## 正式评估合同

每折summary必须证明：`pilot=false`、Git干净、固定fold/seed、同一Git提交、冻结配置和预训练权重哈希、外层测试没有参与训练/早停/阈值/校准、温度与阈值只拟合inner validation、MLflow父子run均存在且结束、权重/后处理/预测/注意力文件均存在且哈希一致。

五折合并后必须证明：恰好覆盖固定registry中的967名患者且无重复；每名患者的二分类参考、fold、图像数与registry一致；总图像数为4,543；每折只使用一个阈值、温度和正式模型ID；注意力覆盖同一批患者和图像且权重合同成立。随后一次性计算预登记性能、校准、稳定性、注意力和资源门槛。失败也写出正式结果并停止，不启动第二候选。

## 验证证据

- G0专项回归：28项通过；
- 项目完整回归：221项通过，76.11秒；
- Python编译检查通过；
- Git差异格式检查通过；
- 文档审计通过：51个Markdown文件、138个相对链接，审计JSON SHA-256为`583a2da6a9a8675d56acbba202bb20264494a32c0a757fd81b4e43c0f59d7954`；
- 代码审查覆盖正确性、外层测试隔离、数据/模型身份、MLflow反查、隐私路径和资源门；未遗留Critical或Important问题；
- dry-run此前已核对967名患者、4,543张图、200正常和767异常，且没有产生训练产物。

## 阶段完成审计

- 文档与代码：本报告引用实现Git和冻结配置哈希；
- 文档与产物：当前明确记录“无G0 MLflow、权重、OOF”，没有以dry-run冒充实验；
- 链接与隐私：主索引、日期日志和实施计划已同步；报告不包含姓名、源文件名或原始数据路径；
- 可复核性：正式评估器将配置、Git、fold/seed、权重、后处理、逐折OOF、注意力、summary和MLflow双向绑定；
- 阴性记录：全量测试首次因120秒外层命令窗口被终止，随后在相同代码、限制4个数学库线程下重跑并全部通过；该过程不是模型实验，也没有训练产物。

## 下一步

从干净Git运行fold 0资源pilot。只检查输入、显存、CPU保护、运行时间、checkpoint及MLflow记录链，不读取或评价outer test。pilot通过并形成独立记录后，才允许从同一个干净Git提交顺序运行五个正式fold。
