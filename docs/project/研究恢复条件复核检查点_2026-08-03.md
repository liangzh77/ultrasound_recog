# 研究恢复条件复核检查点

## 结论

截至2026-08-03，现有冻结数据和S1a临床确认状态均未发生能够重启模型研究的变化。不得恢复E2-C、G0、F0～F2或Z1，也不得提前启动S1a真实复核、S1b、nnU-Net、YOLO或切面模型。

本检查点是只读状态核验，不是正式模型实验，不产生MLflow run、权重、OOF或新诊断结论。

## 核验基线

| 项目 | 值 |
|---|---|
| Git提交 | `9bee73dcd67e504ba75296b4e0f7f1a131cca81d` |
| 冻结数据指纹 | `62ecb01c4d77ec0012704611ecc8d18ef51ebb4e0ea744fb3896948829f0b675` |
| 私有源文件清单SHA-256 | `1142906911c8b1b82448ad6adcb62337012f59316ee8b851a393fdbcbb99fac4` |
| 数据冻结摘要SHA-256 | `14f0284f7538d9d4f63751ffa37ba4d1149632291db853ac1ad210f47c772aef` |
| S1a确认配置SHA-256 | `9abab1f7e2f783f3da451a000c1e5dce959976ab64a2e76073bfeacb86b9d0e7` |

私有清单包含受保护的相对连接信息，因此不在本报告中展开或链接其内容。

## 数据恢复门核验

只对开发队列和2026标签隐藏队列执行文件合同与逐文件SHA-256对账，不解析图像内容，不读取或推断2026参考标准。

| 核验项 | 结果 |
|---|---:|
| 冻结清单文件数 | 16,809 |
| 当前文件数 | 16,809 |
| 新增文件 | 0 |
| 缺失文件 | 0 |
| 大小变化 | 0 |
| 修改时间变化 | 0 |
| 完成SHA-256复核 | 16,809 |
| SHA-256不一致 | 0 |
| 读取错误 | 0 |

逐文件哈希复核运行85.9秒，单线程、GPU未使用。结果证明两个队列仍与冻结清单逐文件一致；没有发现新的带标签数据、采集元数据或对现有内容的替换。

## S1a临床确认恢复门核验

机器可读确认配置仍为`draft_unconfirmed`：

- 八项医学定义均为`pending`，选择、操作定义和理由均未冻结；
- 八项复核参数均为`pending`，最终值和理由均未冻结；
- 临床与研究双签署均不存在，确认日期为空；
- `completed_workbook_sha256`仍为空，没有可进入严格工作簿/YAML对账的已配置回表。

因此S1a机器门仍为`ready=false`。真实双人盲法录入、合并、分歧裁决和一致性统计继续在写入前失败关闭。

## 当前决定

1. 保持现有数据指纹、fold、模型结果和停止决定不变；
2. 不把重复调参、替换骨干或临床融合当作“继续研究”；
3. 2026队列继续隔离，不生成演示预测；
4. 研究保持开放，但只有下列外部事件能触发新的执行阶段：
   - 收到完成且双签署的S1a回表并通过严格机器对账；
   - 收到真正新增的带参考标准数据；
   - 收到可审计的设备、操作者、切面或采集协议元数据。

触发后首先进行只读完整性、泄漏和版本审计，再重新预登记研究问题、数据指纹、fold、候选数与停止门槛；不得继承当前开发集上的顺序调参。

## 隐私与局限性

本报告不含姓名、患者文件名或原始路径。文件哈希一致只能证明本地冻结输入未变化，不能替代独立外部验证，也不能证明客户尚未提供到其他未纳入目录的材料。后续收到任何新输入时必须显式纳入新的受控清单。

检查点完成后的文档一致性审计覆盖62个Markdown、205个相对链接和27项实验登记，全部通过；审计JSON SHA-256为`d944ec7c3ce74bcdc4bd5729c71a5def336a4200d49ec2977da33d4fdb874db3`。
