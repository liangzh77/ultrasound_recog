# 膝关节超声 AI 识别系统 — 开发日志

## 一、项目概述

**目标**：基于已标注的膝关节超声图像数据，构建 AI 系统实现区域分割 + 疾病分类。

**技术路线**：三条并行方案
- 方案 A：YOLO11-seg（端到端实例分割）
- 方案 B：nnU-Net + EfficientNet-B4（语义分割 + 独立分类器）
- 方案 C：MedSAM 微调（基础模型 + 分类头联合训练）

---

## 二、数据探索与分析

### 2.1 数据集基本情况

| 项目 | 数值 |
|------|------|
| 疾病类别 | 7 类（正常、类风湿性关节炎、骨性关节炎、痛风性关节炎、脊柱关节炎、损伤、滑膜囊肿） |
| 患者总数 | 730 |
| 图片总数 | 6866 |
| 标注文件数 | 3420 |
| 标注区域类别 | 86 个（清洗前） |
| 图片分辨率 | 多数为 1280×960 |
| 标注格式 | ISAT（Image Segmentation Annotation Tool），多边形分割 |

### 2.2 数据目录结构

```
data/膝关节已标注/
├── 正常/              # N 前缀
├── 类风湿性关节炎/    # RA 前缀
├── 骨性关节炎/        # OA 前缀
├── 痛风性关节炎/      # GA 前缀
├── 脊柱关节炎/        # SPA- 前缀
├── 损伤/              # 损伤- 前缀
└── 滑膜囊肿/          # 滑膜囊肿- 前缀
    └── 患者文件夹/
        ├── 图片.jpg
        └── 图片.json   # ISAT 标注
```

### 2.3 发现的数据质量问题

通过遍历所有 JSON 标注文件，发现以下问题：

1. **错别字**："内测" 应为 "内侧"、"肌建" 应为 "肌腱"
2. **格式不一致**：`N-内测半月板` 多了连字符，应为 `N内侧半月板`
3. **缺少疾病前缀**：`内测半月板`、`腘窝囊肿` 缺少疾病类别前缀
4. **占位符标签**：`滑膜那种` 应为 `滑膜囊肿-滑膜囊肿`
5. **疑似错误**：`SPA-髌下深囊炎`（保留并标记，需人工确认）
6. **孤立文件**：1 个 JSON 无对应图片，35 张图片无对应 JSON

---

## 三、学术调研

### 3.1 研究的主要方法

| 方法 | 核心思路 | 适用场景 |
|------|----------|----------|
| YOLO11-seg | 单阶段实例分割，速度快 | 实时推理、端到端 |
| nnU-Net v2 | 自配置语义分割框架 | 医学图像分割金标准 |
| MedSAM/SAM2 | 基础模型 + prompt 微调 | 少样本、跨域泛化 |
| Mask R-CNN | 两阶段实例分割 | 精度要求高的场景 |
| TransUNet | Transformer + U-Net | 全局上下文建模 |

### 3.2 研究成果输出

- `研究方案.md`：完整的学术调研报告（25.8 KB）
- `研究方案.html`：苹果风格的 HTML 可视化版本（58.7 KB）
- `研究方案.pdf`：PDF 版本（1036 KB）

---

## 四、工程实现

### 4.1 项目结构设计

```
ultrasound_recog/
├── configs/                    # 训练配置（由脚本自动生成）
├── scripts/                    # 12 个流水线脚本
│   ├── 01_clean_labels.py      # 标签清洗
│   ├── 02_split_dataset.py     # 患者级数据集拆分
│   ├── 03_convert_coco.py      # ISAT → COCO 格式
│   ├── 04_convert_yolo.py      # COCO → YOLO-seg 格式
│   ├── 05_convert_nnunet.py    # ISAT → nnU-Net 格式
│   ├── 06_convert_medsam.py    # ISAT → MedSAM 格式
│   ├── 07_train_yolo.py        # YOLO11m-seg 训练
│   ├── 08_train_nnunet.sh      # nnU-Net v2 训练命令
│   ├── 09_train_classifier.py  # EfficientNet-B4 分类器
│   ├── 10_train_medsam.py      # MedSAM + 分类头联合微调
│   ├── 11_evaluate.py          # 统一评估
│   └── 12_inference.py         # 统一推理 + ONNX 导出
├── src/                        # 可复用模块
│   ├── label_mapping.py        # 标签映射与清洗规则
│   ├── data_utils.py           # 数据加载工具
│   ├── metrics.py              # Dice/IoU/F1/AUC-ROC
│   └── visualize.py            # 可视化工具
└── requirements.txt            # 依赖
```

### 4.2 各模块实现详情

#### 4.2.1 标签映射模块 — `src/label_mapping.py`

定义了标签清洗的核心规则：

```python
LABEL_FIX_MAP = {
    "N内测副韧带": "N内侧副韧带",
    "N-内测半月板": "N内侧半月板",
    "SPA-股二头肌建": "SPA-股二头肌腱",
    "滑膜那种": None,  # 需结合疾病目录补全
}
```

同时定义了 7 类疾病映射、86 个区域类别到疾病的映射关系、以及从标签提取解剖结构的函数。

#### 4.2.2 标签清洗 — `scripts/01_clean_labels.py`

- 遍历 `data/膝关节已标注/` 下所有 JSON
- 应用修复规则（错别字、补前缀、占位符替换）
- 不覆盖原文件，输出到 `data/cleaned/`
- 生成清洗报告 `data/clean_report.json` 和类别映射 `data/category_mapping.json`
- 支持 `--dry-run` 模式

#### 4.2.3 数据集拆分 — `scripts/02_split_dataset.py`

- **患者级拆分**：同一患者的所有图片只出现在同一集合，防止数据泄漏
- **分层抽样**：每个疾病类别内独立拆分，保证各集合疾病分布一致
- **比例**：train 70% / val 15% / test 15%（按患者数）
- 输出 `data/splits/{train,val,test}.txt` + `split_stats.json`

#### 4.2.4 格式转换（scripts/03-06）

| 脚本 | 输入 | 输出 | 说明 |
|------|------|------|------|
| 03_convert_coco.py | 清洗后 JSON + split | `data/coco/{split}/annotations.json` + images/ | 标准 COCO 实例分割格式 |
| 04_convert_yolo.py | COCO annotations | `data/yolo_seg/images/{split}/` + `labels/{split}/` | YOLO-seg 归一化坐标格式 |
| 05_convert_nnunet.py | 清洗后 JSON + split | `data/nnunet/Dataset001_Knee/` | 像素级 PNG 掩码 |
| 06_convert_medsam.py | 清洗后 JSON + split | `data/medsam/{split}/` | 二值掩码 + bbox JSON |

#### 4.2.5 训练脚本（scripts/07-10）

**方案 A — YOLO11-seg**（`07_train_yolo.py`）：
- 模型：YOLO11m-seg 预训练权重
- 输入尺寸：640，epochs: 300，patience: 50
- 疾病分类：检测结果前缀投票机制

**方案 B — nnU-Net + 分类器**（`08_train_nnunet.sh` + `09_train_classifier.py`）：
- 分割：nnU-Net v2 自配置 5-fold 交叉验证
- 分类：EfficientNet-B4，WeightedRandomSampler 处理类别不平衡

**方案 C — MedSAM**（`10_train_medsam.py`）：
- MedSAM ViT-B + 7 类分类头联合模型
- 输入：1024×1024，bbox prompt
- 损失：`λ_seg × (DiceLoss + BCE) + λ_cls × CrossEntropy`
- 分割和分类使用不同学习率（1:10）

#### 4.2.6 评估模块 — `src/metrics.py` + `scripts/11_evaluate.py`

评估指标：
- **分割**：Dice Score、IoU、mAP@0.5、mAP@0.5:0.95
- **分类**：Accuracy、Weighted F1、Confusion Matrix、AUC-ROC
- **可视化**：混淆矩阵热力图、方案对比柱状图、掩码叠加图

#### 4.2.7 推理部署 — `scripts/12_inference.py`

- 统一接口：`--method yolo|nnunet|medsam --image <path>`
- YOLO：完整检测 + 掩码轮廓 + 疾病投票
- nnU-Net：EfficientNet 分类 + nnUNetv2_predict 命令
- 支持 `--export-onnx` 导出 YOLO ONNX 模型
- 输出 JSON 结果文件

---

## 五、标签清洗测试结果

运行 `python scripts/01_clean_labels.py` 的结果：

### 5.1 数据配对情况

| 项目 | 数量 |
|------|------|
| 有效配对样本（图片 + JSON） | 3419 |
| 孤立 JSON（无对应图片） | 1 |
| 缺标注的图片 | 35 |
| 被修改的 JSON 文件数 | 155 |
| 清洗后唯一类别数 | 84（原 86） |

### 5.2 标签修改明细

| 原始标签 | 修正后标签 | 修改次数 | 问题类型 |
|----------|-----------|----------|----------|
| `N内测副韧带` | `N内侧副韧带` | 117 | 错别字（测→侧） |
| `内测半月板` | `N内侧半月板` | 29 | 缺前缀 + 错别字 |
| `滑膜那种` | `滑膜囊肿-滑膜囊肿` | 22 | 占位符文本 |
| `腘窝囊肿` | `滑膜囊肿-腘窝囊肿` | 16 | 缺疾病前缀 |
| `N-内测半月板` | `N内侧半月板` | 12 | 多余连字符 + 错别字 |
| `SPA-股二头肌建` | `SPA-股二头肌腱` | 1 | 错别字（建→腱） |
| **合计** | | **197 处修改** | |

### 5.3 孤立文件

- **孤立 JSON**（1 个）：`正常/DEMO/内侧副韧带.json`（无对应图片）
- **缺标注图片**（35 张）：分布在正常(10)、痛风性关节炎(9)、类风湿性关节炎(14)、骨性关节炎(2)

### 5.4 清洗后类别分布

共 84 个区域类别，按疾病分组：

| 疾病前缀 | 类别数 | 示例 |
|----------|--------|------|
| GA（痛风） | 11 | GA滑膜、GA痛风石、GA结晶 |
| N（正常） | 13 | N内侧副韧带、N软骨、N骨皮质 |
| OA（骨关节炎） | 14 | OA骨赘、OA软骨、OA滑膜 |
| RA（类风湿） | 18 | RA骨侵蚀、RA滑膜血流、RA囊肿 |
| SPA-（脊柱关节炎） | 15 | SPA-骨侵蚀、SPA-鹅足腱滑囊炎 |
| 损伤- | 11 | 损伤-髌前血肿、损伤-滑膜囊肿 |
| 滑膜囊肿- | 2 | 滑膜囊肿-滑膜囊肿、滑膜囊肿-腘窝囊肿 |

---

## 六、Git 提交记录

| 提交 | 说明 | 文件数 |
|------|------|--------|
| `f80bcac` | 添加研究方案（md + html + pdf） | 4 |
| `8d8a099` | 实现完整开发流水线（数据处理 + 训练 + 评估 + 推理） | 19 |

---

## 七、数据处理流水线执行

### 7.1 脚本执行记录

所有数据处理脚本已成功执行：

| 步骤 | 脚本 | 状态 | 关键输出 |
|------|------|------|----------|
| 1 | 01_clean_labels.py | 已完成 | 155 文件修改，197 处标签修正 |
| 2 | 02_split_dataset.py | 已完成 | train 2349 / val 514 / test 556 |
| 3 | 03_convert_coco.py | 已完成 | 8525 个标注区域 |
| 4 | 04_convert_yolo.py | 已完成 | 3419 个标签文件 + yolo_seg.yaml |
| 5 | 05_convert_nnunet.py | 已完成 | Tr 2863 / Ts 556 + dataset.json |
| 6 | 06_convert_medsam.py | 已完成 | 8525 个 mask-bbox 对 |

### 7.2 环境配置

- **Python**: 3.13.7
- **GPU**: NVIDIA GeForce RTX 3080 (10GB VRAM)
- **PyTorch**: 2.6.0+cu124
- **ultralytics**: 8.4.17
- **timm**: 1.0.25

### 7.3 训练参数调整

由于 RTX 3080 10GB 显存限制，YOLO 训练参数做了如下调整：
- `batch`: 8 → 4（避免 OOM）
- `workers`: 4 → 2（Windows 多进程兼容）

---

## 八、模型训练

### 8.1 方案 A — YOLO11-seg

- **状态**：已完成（早停）
- **模型**：YOLO11m-seg（22.4M 参数，113.9 GFLOPs）
- **优化器**：MuSGD(lr=0.01, momentum=0.9)，自动选择
- **配置**：imgsz=640, batch=4, epochs=300, patience=50
- **输出目录**：`runs/yolo_seg/knee_v1/`
- **实际训练**：168 epochs（Epoch 118 为最佳，之后 50 epochs 无提升触发早停）
- **训练耗时**：约 5.2 小时
- **GPU 显存**：~2.8G / 10G（imgsz=640）

#### 训练过程

初始 imgsz=1280 导致 GPU 显存占用 9G/10G，Windows 桌面渲染（DWM）和 Cursor（Electron）因 GPU 资源不足卡死。将 imgsz 调整为 640 后显存降至 2.8G，系统恢复正常。

训练 Loss 变化：

| Epoch | box_loss | seg_loss | cls_loss | dfl_loss |
|-------|----------|----------|----------|----------|
| 1     | 1.539    | 2.581    | 4.196    | 1.437    |
| 50    | 0.888    | 1.209    | 1.024    | 1.015    |
| 118（最佳） | 0.628 | 0.961 | 0.619 | 0.913 |
| 168（最终） | 0.509 | 0.810 | 0.456 | 0.864 |

#### 验证集最佳结果（Epoch 118）

| 指标 | Box | Mask |
|------|-----|------|
| Precision | 0.508 | 0.508 |
| Recall | 0.453 | 0.453 |
| mAP50 | 0.468 | 0.467 |
| mAP50-95 | 0.276 | 0.276 |

#### Test 集评估结果

| 指标 | Box | Mask |
|------|-----|------|
| mAP50 | 0.403 | 0.395 |
| mAP50-95 | 0.281 | 0.238 |

推理速度：7.9ms/图（inference）

#### 各疾病类别 Test 集表现（Mask mAP50）

| 疾病 | 样本数 | 表现 | 代表性类别 |
|------|--------|------|-----------|
| N（正常） | 130 | 较好 | N骨皮质 0.995, N股四头肌腱 0.967, N软骨 0.921 |
| GA（痛风） | 79 | 中等 | GA腘窝囊肿 0.830, GA骨皮质 0.909, GA滑膜 0.066 |
| OA（骨关节炎） | 92 | 中等偏低 | OA囊肿 0.706, OA骨皮质 0.585, OA骨赘 0.130 |
| RA（类风湿） | 156 | 偏低 | RA内侧副韧带 0.995, RA滑膜 0.449, RA骨侵蚀 0.130 |
| SPA-（脊柱关节炎） | 62 | 较低 | SPA-髌韧带 0.922, SPA-滑膜 0.024, SPA-积液 0.042 |
| 损伤 | 20 | 样本极少 | 损伤-积液 0.840, 损伤-髌韧带 0.350 |
| 滑膜囊肿 | 17 | 样本极少 | 未单独评估 |

#### 分析与改进方向

1. **总体 mAP50=0.395**：作为 84 类的首轮基线训练，结果合理
2. **正常组织表现最好**：样本多且结构特征明显（骨皮质、韧带等）
3. **小样本类别表现差**：SPA-、损伤、滑膜囊肿样本不足，模型难以学习
4. **可能的改进**：
   - 合并相似解剖结构类别（减少类别数）
   - 增加小类别数据增强强度
   - 尝试更大模型（YOLO11l-seg）或提高 imgsz
   - 两阶段策略：先分割解剖结构，再分类疾病

### 8.2 方案 B — EfficientNet-B4 分类器

- **状态**：已完成
- **模型**：EfficientNet-B4（timm 预训练，ImageNet）
- **任务**：7 类疾病分类（正常、类风湿、骨关节炎、痛风、脊柱关节炎、损伤、滑膜囊肿）
- **配置**：384×384, batch=8, 100 epochs, lr=1e-4, AdamW + CosineAnnealing
- **类别不平衡处理**：WeightedRandomSampler + 加权 CrossEntropyLoss
- **数据增强**：RandomHorizontalFlip, ColorJitter, RandomAffine
- **GPU 显存**：~6.8G / 10G（batch=8；batch=16 会占满 9.9G 导致卡机）
- **输出**：`runs/classifier/best.pth`

#### 训练过程

初始 batch=16 导致 GPU 显存占用 9.9G/10G，与 YOLO 相同原因会卡机。调整为 batch=8 后显存降至 6.8G。

训练指标变化：

| Epoch | Train Loss | Train Acc | Val Loss | Val Acc |
|-------|-----------|-----------|----------|---------|
| 1     | 1.0626    | 28.86%    | 1.9787   | 25.88%  |
| 10    | 0.1879    | 82.21%    | 1.4479   | 49.42%  |
| 20    | 0.0578    | 93.32%    | 1.5070   | 54.28%  |
| 50    | 0.0100    | 98.85%    | 1.9020   | 59.73%  |
| 80    | 0.0026    | 99.87%    | 2.0830   | 61.28%  |
| 100   | 0.0012    | 99.87%    | 2.1256   | 60.51%  |

Best Val Accuracy: **62.84%**

#### 分析

1. **严重过拟合**：Train Acc 99.87% vs Val Acc 62.84%，泛化能力不足
2. **Val Loss 持续上升**：从 Epoch 10 开始 Val Loss 不断增大（1.45 → 2.13），典型过拟合
3. **可能的改进**：
   - 增加正则化（Dropout、更强的数据增强）
   - 使用 Early Stopping
   - 减小模型（EfficientNet-B0/B2）
   - Label Smoothing

### 8.3 方案 B — nnU-Net 分割

- **状态**：待执行（依赖已安装：nnunetv2 2.6.4）

### 8.4 方案 C — MedSAM

- **状态**：待执行（依赖已安装：segment-anything, monai；需下载 medsam_vit_b.pth 预训练权重）

---

## 九、后续步骤

```
01_clean_labels.py        ✅
02_split_dataset.py       ✅
03_convert_coco.py        ✅
04_convert_yolo.py        ✅
05_convert_nnunet.py      ✅
06_convert_medsam.py      ✅
07_train_yolo.py          ✅ 已完成（168 epochs，早停）
08_train_nnunet.sh        ⏳ 待执行
09_train_classifier.py    ✅ 已完成（100 epochs，Best Val Acc 62.84%）
10_train_medsam.py        ⏳ 待执行
11_evaluate.py            ⏳ 待执行
12_inference.py           ⏳ 待执行
```

---

## 十、技术要点备忘

- **编码问题**：Windows 终端输出中文需设置 `PYTHONIOENCODING=utf-8` 或 `sys.stdout.reconfigure(encoding='utf-8')`
- **患者级拆分**：防止同一患者数据出现在 train 和 test 中导致数据泄漏
- **类别不平衡**：滑膜囊肿仅 36 个标注 vs 类风湿 1006 个，需使用 WeightedRandomSampler 或 Focal Loss
- **MedSAM 输入**：使用 [0, 1] 归一化，不用 ImageNet mean/std
- **nnU-Net**：train + val 合并为 Tr（框架自行做交叉验证），test 作为 Ts
- **GPU 显存限制**：RTX 3080 10GB；YOLO imgsz=1280(9G)、EfficientNet batch=16(9.9G) 都会占满显存导致 Windows 桌面卡死，需降低参数
- **HuggingFace 下载**：国内访问 HuggingFace 需设置 `HF_ENDPOINT=https://hf-mirror.com`
- **Python 3.13 兼容性**：PyTorch 需使用 cu124 索引安装
